spring:
  datasource:
    url: jdbc:mysql://localhost:3306/firstmotorsdb?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 1911
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    open-in-view: false
  sql:
    init:
      platform: mysql
springdoc:
  paths-to-match: /api/**/v1/**
  swagger-ui:
    use-root-path: true
