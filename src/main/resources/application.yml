spring:
  datasource:
    url: jdbc:mysql://localhost:3306/first_motors_database
    username: root
    password: test
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    open-in-view: false
  sql:
    init:
      platform: mysql
springdoc:
  paths-to-match: /api/**/v1/**
  swagger-ui:
    use-root-path: true